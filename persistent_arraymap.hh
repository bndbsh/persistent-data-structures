#ifndef _PERSISTENT_ARRAYMAP_H_
#define _PERSISTENT_ARRAYMAP_H_

#include "conf.hh"
#include "object.hh"
#include <vector>

struct xt_value;
struct XT_State;
namespace xt {

// struct ArrayMap : Object {
// 	std::vector<ObjectHandle> array;
// };
// typedef std::shared_ptr<ArrayMap> ArrayMapHandle;
// namespace map {
// 
// ArrayMapHandle empty();
// ArrayMapHandle assoc(ArrayMapHandle map, ObjectHandle key, ObjectHandle value);
// ObjectHandle get(const ArrayMapHandle& map, ObjectHandle key);
// ObjectHandle getik(const ArrayMapHandle& map, int idx);
// ObjectHandle getiv(const ArrayMapHandle& map, int idx);
// int len(const ArrayMapHandle& map);

// }
}

#endif
